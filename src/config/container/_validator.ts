import 'reflect-metadata';

import {Logger} from '@creatioart-js/express-logging';

/**
 * Register the Validator
 */
export function registerValidator(): void {
  Logger().info(`Register the Validator Rules`);

  return;
}
